export interface title {
  header: string;
  desc: string;
}
export interface skillCard {
  icon: string;
  title: string;
  font_size: string;
}
export interface projectCard {
  contribution: string;
  description: string;
  year: string;
  showWebLink: boolean;
  showGitLink: boolean;
  image: any;
}
